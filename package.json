{
  "name": "cvp-vesting-statistics",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ $GITHUB_USER/cvp-vesting-statistics",
    "create-local": "graph create --node http://localhost:8020/ $GITHUB_USER/cvp-vesting-statistics",
    "remove-local": "graph remove --node http://localhost:8020/ $GITHUB_USER/cvp-vesting-statistics",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 $GITHUB_USER/cvp-vesting-statistics"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
